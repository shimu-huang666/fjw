# 光电测试技术作业题目和答案

## 一、教材 P50：思考题与习题 1

### 1.1 题目
用目视观察发光波长分别为 435.8 nm 和 546.1 nm 的两个发光体，它们的亮度相同，均为 3 cd/m^2，如果在两个发光体前分别加上透射比为 10^-4 的光衰减器，问此时目视观察的亮度是否相同？为什么？

### 1.1 答案
不一定相同。

加衰减器后，按光度学计算二者亮度都变为：

```text
L' = 3 x 10^-4 cd/m^2
```

但此时亮度很低，人眼由明视觉逐渐转向暗视觉或中间视觉。人眼光谱灵敏度曲线会向短波方向移动，即浦肯野效应。因此虽然两束光的光度学亮度按同一比例减小，但人眼对 435.8 nm 和 546.1 nm 的相对视觉灵敏度发生变化，主观观察到的亮度不再相同。短波蓝光的相对亮度会比明视觉时提高。

### 1.6 题目
假设一只白炽灯各向发光均匀，悬挂在离地面 1.5 m 的高度上，用照度计测得正下方地面上照度为 30 lx，求该白炽灯的光通量。

### 1.6 答案
正下方地面处，入射光近似垂直：

```text
E = I / r^2
```

所以发光强度：

```text
I = E r^2 = 30 x 1.5^2 = 67.5 cd
```

白炽灯各向均匀发光，总光通量为：

```text
Phi = 4 pi I = 4 pi x 67.5 = 270 pi lm
```

因此：

```text
Phi ≈ 848 lm
```

答：该白炽灯光通量约为 848 lm。

## 二、教材 P161：思考题与习题 4

### 4.1 题目
彩度或饱和度最高的颜色是什么？彩度最低的颜色是什么？当两辐射波长相同而彩度不同时，我们说它们什么相同？

### 4.1 答案
彩度或饱和度最高的颜色是单色光对应的光谱色，也可称为纯色。

彩度最低的颜色是非彩色，即白色、灰色、黑色等。

当两种辐射波长相同而彩度不同时，说明它们的色调相同，只是颜色纯度或饱和度不同。

### 4.2 题目
何为三原色？三原色有多少种？三原色的定义有什么条件？

### 4.2 答案
三原色是指可以通过不同比例混合来匹配许多颜色的三种相互独立的基色。常用的加色三原色是红、绿、蓝。

三原色不是唯一的，理论上有很多组三原色。只要三种颜色相互独立，就可以作为一组三原色。

三原色应满足的条件：

1. 三种颜色中任意一种不能由另外两种混合得到。
2. 三种颜色相互独立。
3. 三种颜色按不同比例混合后，应能匹配尽可能多的颜色。

### 4.3 题目
色度学中，CIE1931-XYZ 色度系统中 X、Y、Z；x、y、z 和 xbar(lambda)、ybar(lambda)、zbar(lambda) 的含义各是什么？它们有什么差异？

### 4.3 答案
X、Y、Z 是 CIE1931-XYZ 色度系统中的三刺激值，表示待测颜色用三个假想原色匹配时所需的数量。其中 Y 与光亮度有关。

x、y、z 是色品坐标，表示颜色的色品：

```text
x = X / (X + Y + Z)
y = Y / (X + Y + Z)
z = Z / (X + Y + Z)
```

并且：

```text
x + y + z = 1
```

xbar(lambda)、ybar(lambda)、zbar(lambda) 是 CIE1931 标准观察者的光谱三刺激值函数，也叫配色函数，用来根据光谱功率分布计算 X、Y、Z。

三者差异：

1. X、Y、Z 是具体颜色刺激的三刺激值，既与颜色有关，也与光强有关。
2. x、y、z 是归一化后的色品坐标，主要表示颜色色品，不直接表示亮度大小。
3. xbar(lambda)、ybar(lambda)、zbar(lambda) 是标准函数，表示不同波长单色光对三刺激值的贡献。

### 4.4 题目
已知光源的相对光谱功率分布 s(lambda)，有色玻璃的光谱透射比 tau(lambda)，试求该有色玻璃的色品坐标和积分透射比。

### 4.4 答案
透过有色玻璃后的相对光谱功率分布为：

```text
s'(lambda) = s(lambda) tau(lambda)
```

用 CIE1931 标准观察者配色函数计算三刺激值：

```text
X = k ∫ s(lambda) tau(lambda) xbar(lambda) d lambda
Y = k ∫ s(lambda) tau(lambda) ybar(lambda) d lambda
Z = k ∫ s(lambda) tau(lambda) zbar(lambda) d lambda
```

其中 k 为归一化常数。

色品坐标为：

```text
x = X / (X + Y + Z)
y = Y / (X + Y + Z)
z = Z / (X + Y + Z)
```

通常只需给出 x、y，因为：

```text
z = 1 - x - y
```

积分透射比可按光视觉加权求：

```text
tau_v = [∫ s(lambda) tau(lambda) ybar(lambda) d lambda] / [∫ s(lambda) ybar(lambda) d lambda]
```

即透射后的 Y 值与入射前 Y 值之比。

## 三、教材 P196：思考题与习题 5

### 5.3 题目
什么是时间相干性和空间相干性？为什么激光的时间相干性和空间相干性都优于其他光源？

### 5.3 答案
时间相干性是指同一光场在同一空间点、不同时刻的相位相关程度。它反映光源的单色性，谱线越窄，相干时间和相干长度越长，时间相干性越好。

空间相干性是指同一时刻光场中不同空间点之间的相位相关程度。它反映波前一致性和方向性，光束发散角越小、波前越规则，空间相干性越好。

激光由受激辐射产生，并由光学谐振腔选模，具有谱线窄、方向性好、波前规则、相位关系稳定等特点，所以激光的时间相干性和空间相干性都明显优于普通光源。

### 5.8 题目
光学多普勒效应与声学多普勒效应的差别何在？

### 5.8 答案
二者本质上都是由于光源、声源、观察者或散射体之间存在相对运动，使接收频率发生变化。

主要区别：

1. 光学多普勒效应对应电磁波，传播不需要介质；声学多普勒效应对应机械波，传播需要介质。
2. 光波波长很短，同样速度下多普勒频移较大，适合高精度速度测量。
3. 光频本身极高，探测器不能直接响应光频变化，通常要用干涉或外差方法把频移转换成低频拍频信号。
4. 声学多普勒可由声学换能器直接接收频率变化，但空间分辨率通常低于光学方法。
5. 光学多普勒适合非接触、高精度、小区域速度测量；声学多普勒适合在气体、液体、人体组织等介质中测量。

### 5.10 题目
比较激光相位测距和激光脉冲测距的技术特点。

### 5.10 答案
激光相位测距是对连续激光进行强度调制，测量发射调制信号与接收调制信号之间的相位差来求距离。其特点是精度高、适合中短距离精密测量，但存在整周模糊，需要通过多个调制频率或其他方法确定整周数。

激光脉冲测距是发射窄激光脉冲，测量脉冲往返飞行时间来求距离：

```text
L = c t / 2
```

其特点是原理直观、测程远、适合远距离和快速目标测量，但精度受脉冲宽度、计时精度、探测器响应速度等限制，一般精度低于相位测距。

比较：

| 项目 | 激光相位测距 | 激光脉冲测距 |
|---|---|---|
| 测量量 | 相位差 | 飞行时间 |
| 光源形式 | 连续调制光 | 短脉冲光 |
| 精度 | 较高 | 相对较低 |
| 测程 | 中短距离较常用 | 远距离较常用 |
| 主要问题 | 相位整周模糊 | 计时精度和脉冲宽度限制 |
| 适用场合 | 精密测距 | 远距离测距、激光雷达 |

## 四、课件图片：光学时间分辨测量技术作业

### 题 1
画图并简述电光调制的原理。

### 答案
简图：

```text
入射光 -> 起偏器 -> 电光晶体(外加调制电压 U) -> 检偏器 -> 调制光输出
                         ↑
                    调制信号 u(t)
```

原理：某些晶体在外加电场作用下折射率会发生变化，这称为电光效应。入射线偏振光通过电光晶体后，由于折射率变化产生相位延迟变化。再经过检偏器后，相位变化可转换为光强变化，从而实现光信号调制。

若外加电压随时间变化：

```text
U = U(t)
```

则晶体折射率、相位差和输出光强也随时间变化，实现对光的调制。

### 题 2
试述锁相放大技术的基本原理。

### 答案
锁相放大技术利用参考信号从强噪声背景中提取与参考信号同频、同相或固定相位关系的微弱信号。

设被测信号为：

```text
u_s = U_s cos(omega t + phi)
```

参考信号为：

```text
u_r = U_r cos(omega t)
```

二者相乘后：

```text
u_s u_r = 1/2 U_s U_r cos phi + 1/2 U_s U_r cos(2 omega t + phi)
```

经过低通滤波器后，高频项被滤除，只剩：

```text
U_out ∝ U_s cos phi
```

因此，锁相放大器只放大与参考频率相关的信号，能有效抑制随机噪声和其他频率的干扰。常与光调制器、斩光器配合使用。

### 题 3
对于变化速度远大于探测器响应速度的过程，应如何进行测量？简述思路。

### 答案
可采用光学时间分辨测量方法，典型方法是泵浦-探测技术或取样测量。

基本思路：

1. 用超短泵浦光脉冲激发样品，使样品产生快速变化。
2. 用另一束较弱的探测光脉冲在延迟时间 Delta t 后照射样品。
3. 改变泵浦光和探测光之间的延迟时间 Delta t。
4. 每个延迟时间只测量该时刻的瞬态状态。
5. 将不同 Delta t 下的探测信号组合起来，重建快速变化过程。

这种方法不是要求探测器直接跟上超快变化，而是利用重复过程和可调延迟，把超快过程转化为逐点取样测量。

