# 光电测试技术作业题目和答案（按教材整理版）

说明：依据《光电测试技术 第3版》（范志刚等）扫描版整理。教材 PDF 无文字层，页码为书内页码；括号内给出对应 PDF 页。为便于核对，每题附“教材依据”和少量短句直接引用。

## 一、教材 P50：思考题与习题 1

### 1.1 题目
用目视观察发光波长分别为 435.8 nm 和 546.1 nm 的两个发光体，它们的亮度相同，均为 3 cd/m^2，如果在两个发光体前分别加上透射比为 10^-4 的光衰减器，问此时目视观察的亮度是否相同？为什么？

### 1.1 答案
不一定相同。

加光衰减器后，若只按光度学亮度作比例计算，两者都会变为：

```text
L' = 3 x 10^-4 cd/m^2
```

但题目问的是“目视观察”的亮度。衰减后亮度已经很低，人眼视觉状态会由明视觉转向暗视觉或中间视觉；人眼对不同波长的相对灵敏度不再与明视觉相同。教材给出的光谱光视效率曲线表明，明视觉峰值在 555 nm 附近，暗视觉峰值在 507 nm 附近。因此短波方向的 435.8 nm 相对视觉效应会增强，546.1 nm 与 435.8 nm 的主观亮度不再保持原来的相等关系。

结论：衰减后用目视观察，两个发光体的亮度通常不再相同；435.8 nm 的短波光相对会显得更亮一些，这是浦肯野效应的表现。

**课件解析**

课件第二章把“辐射度量”和“光度量”区分开来：辐射量描述客观光功率，光度量要按人眼视觉响应加权。课件中光视效能定义为“人眼对某一波长下单位辐射通量产生的光通量”，并强调不同波长即使辐射功率相同，转换成光通量后的视觉贡献也不同。题中两个波长一开始按明视觉亮度相同，但加上 `10^-4` 衰减后进入低亮度观察条件，视觉响应状态改变，所以不能只用同一个透射比判断“目视亮度仍相同”。解题关键是：光度学亮度数值同倍缩小，不等于人眼主观亮度比例仍保持不变。

课件出处：第二章课件第 31-32 页（光度量、光视效能），第三章课件第 37、39 页（不同波长辐射通量转换为光通量的视觉权重差异）。

**教材依据**

- 教材 P10（PDF 第 20 页）：光度学量说明中写到，直接引用：“光度学量只在可见光谱范围内才有意义”。
- 教材 P11（PDF 第 21 页）：明视觉光谱光视效率最大值，直接引用：“峰值在555nm处”。
- 教材 P11（PDF 第 21 页）：暗视觉光谱光视效率最大值，直接引用：“峰值在507nm处”。
- 教材 P50（PDF 第 60 页）：本题题干给出 435.8 nm、546.1 nm、3 cd/m^2 和 10^-4 的条件。

### 1.6 题目
假设一只白炽灯各向发光均匀，悬挂在离地面 1.5 m 的高度上，用照度计测得正下方地面上照度为 30 lx，求该白炽灯的光通量。

### 1.6 答案
白炽灯各向发光均匀，可近似看作点光源。正下方地面处，入射方向近似垂直，所以：

```text
E = I / r^2
```

代入：

```text
I = E r^2 = 30 x 1.5^2 = 67.5 cd
```

各向均匀发光时，总光通量为：

```text
Phi = 4 pi I = 4 pi x 67.5 = 270 pi lm
```

因此：

```text
Phi ≈ 848 lm
```

答：该白炽灯的光通量约为 848 lm。

**课件解析**

课件第二章的光度量表把发光强度、光通量和照度联系起来：发光强度用于描述点光源在某方向单位立体角内的光通量，照度用于描述接收面单位面积接收的光通量。正下方地面近似垂直接收，因此可用平方反比关系 `E = I / r^2` 先求发光强度。题目又说明白炽灯各向均匀发光，课件中这类光源可按整个球面立体角 `4pi` 处理，所以总光通量为 `Phi = 4pi I`。这道题的核心不是复杂光源模型，而是把“接收面照度”反推为“光源发光强度”，再由各向均匀得到总光通量。

课件出处：第二章课件第 31 页（光通量、发光强度、照度的对应定义），第二章课件第 34 页（接收器上光度计量对应照度）。

**教材依据**

- 教材 P10（PDF 第 20 页）：发光强度的定义式为 `I = dPhi / dOmega`。
- 教材 P10（PDF 第 20 页）：照度的定义式为 `E = dPhi / dA`。
- 教材 P10（PDF 第 20 页）：直接引用：“光通量的单位是流明(lm)”。
- 教材 P50（PDF 第 60 页）：题目 1.6 给出“向各方向发光均匀”和 `1.5 m`、`30 lx`。

## 二、教材 P161：思考题与习题 4

### 4.1 题目
彩度或饱和度最高的颜色是什么？彩度最低的颜色是什么？当两辐射波长相同而彩度不同时，我们说它们什么相同？

### 4.1 答案
彩度或饱和度最高的是单色光对应的光谱色，也可称为纯色。

彩度最低的是非彩色，典型为白色、灰色、黑色；其中白光的彩度最低。

当两种辐射波长相同而彩度不同时，说明它们的色调相同，只是颜色纯度或饱和度不同。

**课件解析**

课件第三章把颜色属性拆成明度、色调和彩度/饱和度。彩度表示颜色的浓淡程度或纯洁性，课件原话中还强调“彩度就是色彩中所含彩色与消色部分（黑/白/灰）比例”。因此单色光不掺白光或灰成分，颜色最纯，彩度最高；白、灰、黑属于消色或非彩色，彩度最低。当辐射波长相同，只是彩度不同，说明主导波长决定的色调未变，只是掺入消色成分多少不同，所以应答“色调相同”。

课件出处：第三章课件第 13-15 页（彩度/饱和度、白色加入对亮度和彩度的影响），第 60 页（色品坐标与色调、彩度的关系）。

**教材依据**

- 教材 P140（PDF 第 150 页）：颜色三属性，直接引用：“明度、色调、彩度（或饱和度）”。
- 教材 P140（PDF 第 150 页）：直接引用：“单色光的彩度最高”。
- 教材 P140（PDF 第 150 页）：直接引用：“白光的彩度最低”。

### 4.2 题目
何为三原色？三原色有多少种？三原色的定义有什么条件？

### 4.2 答案
三原色是用于颜色匹配的三种相互独立的基色。三原色不是唯一的，理论上可以有多组；常用加色三原色为红、绿、蓝。

三原色应满足：

1. 三种颜色相互独立。
2. 任意一种不能由另外两种混合得到。
3. 三种颜色按不同比例混合后，应能匹配尽可能多的颜色。

**课件解析**

课件第三章专门给出“三原色”的判断条件：在颜色匹配实验中，为匹配某一种颜色，一般需要三种独立颜色；三原色可以任意选定，但任一原色不能由另外两种原色相加混合得到；最常用的是红、绿、蓝三原色。所以这题不能简单答“红绿蓝就是唯一三原色”，而要说明三原色是颜色匹配用的独立基色，理论上不唯一。课件还把色光三原色 RGB 与色料三原色 CMY 分开，考试答题时要注意这里问的是色度学/加色匹配意义下的三原色。

课件出处：第三章课件第 25、28 页（色料三原色与色光三原色），第 34 页（三原色定义、任意选定、独立性条件）。

**教材依据**

- 教材 P140（PDF 第 150 页）：颜色环和混合规律说明，直接引用：“红、绿、蓝三色是最适当的颜色”。
- 教材 P144（PDF 第 154 页）：CIE1931-RGB 系统，直接引用：“3种光谱色为三原色”。
- 教材 P144（PDF 第 154 页）：三原色波长给出红 700 nm、绿 546.1 nm、蓝 435.8 nm。

### 4.3 题目
色度学中，CIE1931-XYZ 色度系统中 X、Y、Z；x、y、z 和 xbar(lambda)、ybar(lambda)、zbar(lambda) 的含义各是什么？它们有什么差异？

### 4.3 答案
`X、Y、Z` 是 CIE1931-XYZ 色度系统中的三刺激值，表示待测颜色由三个假想原色匹配时所需的数量。其中 `Y` 与亮度对应。

`x、y、z` 是色品坐标，是三刺激值归一化后的结果：

```text
x = X / (X + Y + Z)
y = Y / (X + Y + Z)
z = Z / (X + Y + Z)
```

并且：

```text
x + y + z = 1
```

`xbar(lambda)、ybar(lambda)、zbar(lambda)` 是 CIE1931 标准色度观察者光谱三刺激值函数，用于由光谱功率分布计算三刺激值。

三者差异：

1. `X、Y、Z` 是具体颜色刺激的三刺激值，既与色有关，也与强度有关。
2. `x、y、z` 是归一化色品坐标，主要表示颜色色品，不直接表示亮度大小。
3. `xbar(lambda)、ybar(lambda)、zbar(lambda)` 是标准观察者函数，表示不同波长单色光对三刺激值的贡献。

**课件解析**

课件第三章的逻辑顺序是“三原色 -> 三刺激值 -> 光谱三刺激值/颜色匹配函数 -> 色品坐标”。三刺激值是匹配某种颜色所需三原色的量，因此 `X、Y、Z` 对应具体颜色刺激的大小；光谱三刺激值是波长的函数，是计算任意光谱颜色的基础；色品坐标则是把三刺激值归一化，课件明确说色品坐标用于“描述颜色的比例，而非绝对亮度”。所以解析时要抓住层级差别：`xbar、ybar、zbar` 是标准函数，`X、Y、Z` 是算出来的三刺激值，`x、y、z` 是由三刺激值归一化后的色品描述。

课件出处：第三章课件第 35 页（三刺激值），第 51-52 页（光谱三刺激值/颜色匹配函数与积分计算），第 56-57 页（色品坐标归一化），第 78-79 页（三刺激值与色品坐标复习页）。

**教材依据**

- 教材 P145（PDF 第 155 页）：XYZ 系统要求，直接引用：“用Y刺激值表示颜色的亮度”。
- 教材 P146（PDF 第 156 页）：给出 RGB 到 XYZ 的换算关系。
- 教材 P147（PDF 第 157 页）：直接引用：“CIE1931标准色度观察者光谱三刺激值”。
- 教材 P147（PDF 第 157 页）：给出 `x(lambda)、y(lambda)、z(lambda)` 与光谱三刺激值的归一化关系。

### 4.4 题目
已知光源的相对光谱功率分布 s(lambda)，有色玻璃的光谱透射比 tau(lambda)，试求该有色玻璃的色品坐标和积分透射比。

### 4.4 答案
透过有色玻璃后的颜色刺激函数为：

```text
phi(lambda) = s(lambda) tau(lambda)
```

用 CIE1931 标准色度观察者光谱三刺激值函数计算：

```text
X = k ∫ s(lambda) tau(lambda) xbar(lambda) d lambda
Y = k ∫ s(lambda) tau(lambda) ybar(lambda) d lambda
Z = k ∫ s(lambda) tau(lambda) zbar(lambda) d lambda
```

色品坐标为：

```text
x = X / (X + Y + Z)
y = Y / (X + Y + Z)
z = Z / (X + Y + Z)
```

通常给出 `x、y` 即可，因为：

```text
z = 1 - x - y
```

积分透射比为透过滤光片后的光通量与透过滤光片前的光通量之比：

```text
tau = Phi / Phi0 = Y / Y0
```

若按教材中把入射光源的 `Y0` 归一化为 100 的处理，则：

```text
tau = Y / 100
```

**课件解析**

课件第三章把分光光度计测色和三刺激值计算放在一起理解：物体色不是只由光源决定，还与物体的光谱透射率或反射率有关。对透射物体，透过后的光谱应写成光源光谱 `s(lambda)` 与透射比 `tau(lambda)` 的乘积，再乘以 CIE 标准观察者的光谱三刺激值函数并积分，得到 `X、Y、Z`。课件还强调色品坐标是由三刺激值归一化得到，所以先算三刺激值，再算 `x、y`。积分透射比本质上是透射前后按视觉加权后的光通量比，因此可用 `Y/Y0` 表示，而不是简单对 `tau(lambda)` 作普通算术平均。

课件出处：第三章课件第 55 页（透射物体 `phi(lambda)=s(lambda)tau(lambda)`），第 87-89 页（色品坐标计算），第 113、118、121 页（分光光度法测量物体颜色、总透射比/光通量比）。

**教材依据**

- 教材 P149（PDF 第 159 页）：透射物体颜色刺激函数，直接引用：“phi(lambda)=s(lambda)tau(lambda)”。
- 教材 P149（PDF 第 159 页）：给出 `X、Y、Z` 的积分计算式。
- 教材 P153（PDF 第 163 页）：积分透射比，直接引用：“tau=Phi/Phi0=Y/Y0”。
- 教材 P154（PDF 第 164 页）：归一化后，直接引用：“tau=Y/100”。

## 三、教材 P196：思考题与习题 5

### 5.3 题目
什么是时间相干性和空间相干性？为什么激光的时间相干性和空间相干性都优于其他光源？

### 5.3 答案
时间相干性是同一空间点上，同一光源分出的两束光在不同时间仍保持确定相位关系、能够产生干涉的能力。它反映光源的单色性。光谱线宽越窄，相干时间和相干长度越长，时间相干性越好。

空间相干性是同一时刻，空间不同点发出的光波之间的相干程度。它反映光源横向尺寸、方向性和波前一致性。光源越接近点光源、发散角越小、波前越规则，空间相干性越好。

激光优于普通光源的原因：

1. 激光谱线窄，单色性好，所以时间相干性好。
2. 激光发散角小，方向性好，等效光源尺寸小，所以空间相干性好。
3. 激光由受激辐射产生，并经谐振腔选模，光场相位关系更稳定。

**课件解析**

课件第六章在讲干涉条件时把相干性分成两类：光源的单色性对应时间相干性，光源大小对应空间相干性。课件总结为“光谱带宽越大，相干长度越短”，以及“光源尺寸越大，不同点源条纹越易错位”。激光的谱线窄、方向性好、等效光源尺寸小，所以更容易满足干涉测量对时间相干性和空间相干性的要求。答题时不要只说“激光好”，要分别从谱宽和光源尺寸/方向性两个维度说明。

课件出处：第五章课件第 4 页（时间相干性、空间相干性定义），第六章课件第 17-22 页（单色性与时间相干性、光源大小与空间相干性、相干性总结）。

**教材依据**

- 教材 P162（PDF 第 172 页）：直接引用：“激光是一种高亮度的定向能束”。
- 教材 P163（PDF 第 173 页）：单色性判断，直接引用：“线宽越窄，光的单色性越好”。
- 教材 P164（PDF 第 174 页）：空间相干性，直接引用：“同一时间，由空间不同点发出的光波的相干性”。
- 教材 P165（PDF 第 175 页）：单模激光器空间相干性，直接引用：“空间相干性很好”。

### 5.8 题目
光学多普勒效应与声学多普勒效应的差别何在？

### 5.8 答案
二者共同点是：当波源、观察者或散射体之间存在相对运动时，接收频率会发生变化。

主要差别：

1. 声波是机械波，传播必须依赖介质；光波是电磁波，传播不依赖机械介质。
2. 声学多普勒效应不仅与声源和观察者有关，还与二者相对于介质的运动有关。
3. 光学多普勒效应主要由光源和观察者之间的相对运动决定。
4. 光频极高，光电探测器不能直接响应光频本身，实际测量常通过拍频、外差或散射光频移得到低频信号。
5. 光学多普勒可用于非接触、高空间分辨率测速；声学多普勒更适合气体、液体、人体组织等介质中的速度测量。

**课件解析**

课件第五章在激光多普勒测速部分直接比较声学和光学多普勒：声学多普勒有纵向效应，但没有横向多普勒效应；光学多普勒除了纵向效应，还有来自相对论时间膨胀的横向效应。课件还给出双光束激光多普勒测速的思路：运动目标使两束散射光产生不同多普勒频移，探测器接收两束光的拍频，由频率差求速度。因此这题的重点是“本质差别”和“检测方式”：声波依赖介质，光波不依赖介质；光学测速通常测的是散射光频率差/拍频，而不是直接测光频。

课件出处：第五章课件第 68-76 页（声学/光学多普勒效应差别、横向光学多普勒），第 77-80 页（双光束激光多普勒测速与拍频测速）。

**教材依据**

- 教材 P180（PDF 第 190 页）：声波特性，直接引用：“声波是依赖于介质传播的”。
- 教材 P180（PDF 第 190 页）：光学与声学差别，直接引用：“有本质的区别”。
- 教材 P183（PDF 第 193 页）：激光多普勒测速中由两束散射光频率差得到速度。
- 教材 P183（PDF 第 193 页）：光电探测限制，直接引用：“光电检测器对光频不能响应”。

### 5.10 题目
比较激光相位测距和激光脉冲测距的技术特点。

### 5.10 答案
激光相位测距：对连续激光进行强度调制，测量发射调制波与接收调制波的相位差来求距离。其特点是精度高，适合中短距离精密测距；但存在相位整周模糊，需要用多个测尺频率或其他方法确定整周数。

激光脉冲测距：发射窄激光脉冲，测量脉冲往返飞行时间来求距离：

```text
L = c t / 2
```

其特点是原理直观、测程远，适合远距离目标；但精度受脉冲宽度、计时精度、探测器响应速度等限制，一般低于相位测距。

比较如下：

| 项目 | 激光相位测距 | 激光脉冲测距 |
|---|---|---|
| 测量量 | 相位差 | 飞行时间 |
| 光源形式 | 连续调制光 | 短脉冲光 |
| 典型公式 | `L = lambda/2 (m + Delta m)` | `L = c t / 2` |
| 精度 | 较高 | 相对较低 |
| 测程 | 中短距离精密测量常用 | 远距离测量常用 |
| 主要问题 | 相位整周模糊、测尺选择 | 计时精度和脉冲宽度限制 |
| 适用场合 | 精密测距、短程/中程测距 | 远距离测距、激光雷达 |

**课件解析**

课件第五章把测距归入“时间/频率编码”：相位测距是比较发射调制信号和返回调制信号的相位差，测尺由调制频率决定，因此会出现整周模糊和量程问题；脉冲测距则直接测飞行时间，公式思路是往返时间乘以光速再除以 2。课件重点强调“激光相位测距量程和测尺频率的关系”以及“激光脉冲测距的原理”。所以比较时要围绕测量量不同来写：相位法测相位、精度高但需解决整周数；脉冲法测时间、测程远但受脉宽和计时分辨率限制。

课件出处：第五章课件第 45-56 页（激光相位测距原理、测尺频率、间接测尺），第 57-58 页（激光脉冲测距原理与分辨力）。

**教材依据**

- 教材 P187（PDF 第 197 页）：测距分类，直接引用：“激光相位测距和脉冲激光测距两种”。
- 教材 P188（PDF 第 198 页）：相位测距公式 `L = lambda/2 (m + Delta m)`。
- 教材 P190（PDF 第 200 页）：脉冲测距，直接引用：“利用激光脉冲连续时间极短”。
- 教材 P190（PDF 第 200 页）：脉冲测距基本式 `L = ct/2`。
- 教材 P191（PDF 第 201 页）：脉冲测距分辨率，直接引用：“分辨率一般较低”。

## 四、课件图片：光学时间分辨测量技术作业

说明：以下 3 题来自课件图片。经核对本教材目录，本书直接覆盖了激光、测距、多普勒、干涉、衍射、莫尔、图像、光纤、层析等测试技术，但未见“电光调制”“锁相放大”“泵浦-探测时间分辨测量”的直接专题。因此下面按课程课件知识组织答案，不伪造教材直接引用。

### 题 1
画图并简述电光调制的原理。

### 答案
简图：

```text
入射光 -> 起偏器 -> 电光晶体(外加调制电压 U) -> 检偏器 -> 调制光输出
                         ↑
                    调制信号 u(t)
```

原理：某些晶体在外加电场作用下折射率会发生变化，即电光效应。线偏振光通过电光晶体后，由于折射率随外加电压变化而产生相位延迟变化。再经检偏器后，相位变化可转换为光强变化，从而实现光信号调制。

若外加电压为：

```text
U = U(t)
```

则晶体折射率、相位差和输出光强也随时间变化，实现对光的振幅、相位或偏振状态的调制。

**课件解析**

课件第四章将光信号调制方式概括为振幅、频率、相位和脉冲调制。电光调制的核心是电场改变晶体折射率，使通过晶体的偏振光产生相位延迟；如果再配合起偏器、检偏器，相位延迟的变化就能转换为输出光强变化。课件中还区分泡克尔斯效应和克尔效应，前者是一阶电光效应，后者是二阶电光效应。画图时必须体现“外加电压作用于电光晶体”和“偏振器件把相位变化转为强度变化”这两点。

课件出处：第四章-1 课件第 1-7 页（调制方式概览），第 30-40 页（电光调制、泡克尔斯效应、克尔效应与相位延迟）。

**教材对应情况**

- 本教材未见电光调制直接专题；该题应以课件“光信号调制”部分为主要依据。

### 题 2
试述锁相放大技术的基本原理。

### 答案
锁相放大技术利用参考信号，从强噪声背景中提取与参考信号同频、同相或固定相位关系的微弱信号。

设被测信号为：

```text
u_s = U_s cos(omega t + phi)
```

参考信号为：

```text
u_r = U_r cos(omega t)
```

二者相乘：

```text
u_s u_r = 1/2 U_s U_r cos phi + 1/2 U_s U_r cos(2 omega t + phi)
```

经过低通滤波后，高频项被滤除，只剩：

```text
U_out ∝ U_s cos phi
```

因此锁相放大器只保留与参考频率相关的信号，能抑制随机噪声和其他频率干扰。光电检测中常配合斩光器或调制器使用，把待测弱光信号搬移到已知调制频率附近，再进行相敏检波和窄带滤波。

**课件解析**

课件第四章把锁相放大列为微弱光电信号检测重点，并强调它通过调制和相敏检波压缩有效信号带宽。调制盘或斩光器的作用，是把连续或缓变光信号变成已知频率的周期信号，同时抑制背景和光源波动。锁相放大器再用同频参考信号相乘，低通后只留下与参考同频且相位相关的直流分量。解题时可以按“三步”写：先调制、再相敏检波、最后低通滤波；这样能说明为什么它能从强噪声背景中取出微弱信号。

课件出处：第四章-1 课件第 7、22 页（锁相放大与斩光调制），第 66 页（调制盘作用）；第四章-3 课件第 73 页以后（锁相放大器用于微弱信号测量）。

**教材对应情况**

- 本教材未见锁相放大直接专题；该题应以课件“微弱光电信号检测技术”部分为主要依据。

### 题 3
对于变化速度远大于探测器响应速度的过程，应如何进行测量？简述思路。

### 答案
可采用光学时间分辨测量，典型方法是泵浦-探测技术或取样测量。

基本思路：

1. 用超短泵浦光脉冲激发样品，使样品产生快速变化。
2. 用另一束较弱的探测光脉冲在延迟时间 `Delta t` 后照射样品。
3. 改变泵浦光和探测光之间的延迟时间 `Delta t`。
4. 每个延迟时间只测量该时刻的瞬态状态。
5. 将不同 `Delta t` 下的探测信号组合起来，重建快速变化过程。

这种方法不是要求探测器直接跟上超快变化，而是利用重复过程和可调光学延迟，把超快时间变化转化为逐点取样测量。

**课件解析**

课件第四章的时间分辨测量部分强调“响应时间决定时间分辨率”。当过程比探测器响应更快时，不能靠普通探测器实时记录完整波形，而要用时间宽度远窄于被测过程的超短光脉冲作为探针。泵浦-探测技术中，泵浦光先激发样品，探测光以不同延迟到达样品；光学延迟线把时间延迟量转换为空间变化量。课件题中“探测光一般弱于泵浦光”“利用光学延迟线”“不能测不可逆过程”等点都服务于同一思路：对可重复的超快过程逐点取样，再拼出随时间变化的过程。

课件出处：第四章-3 课件第 34 页（响应时间决定时间分辨率），第 57-72 页（超短光脉冲探针、光学延迟线、泵浦-探测技术），第 94 页（泵浦-探测相关判断题）。

**教材对应情况**

- 本教材未见泵浦-探测时间分辨测量直接专题；该题应以课件“光学时间分辨测量技术”部分为主要依据。
